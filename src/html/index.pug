extends /_templates/_layout

block vars
  - var page         = {}
  - page.title       = ""
  - page.keywords    = ""
  - page.description = ""
  - var filePath     = "/" //- 必須。詳しくはreadmeを参照

block content
  .items.js-parallax
    - for (var i = 0; i < 7; i++)
      .item(class=`item--${i}`, data-depth=`${i * 0.2}`)
